import sqlite3
# Connect to the database
connection = sqlite3.connect('Kuwait_landmarks.db')
cursor = connection.cursor()
# Data Retrieval
cursor.execute(                     )
# Fetch the Data

# Output the Data

# Close the database
connection.close()
